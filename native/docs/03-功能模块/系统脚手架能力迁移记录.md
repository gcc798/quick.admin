# 系统脚手架能力迁移记录

## 目标

将 `/Users/guoc/dev/code_go/src/lsh-app/native` 和 `/Users/guoc/dev/code_go/src/lsh-app/web-react` 中已经沉淀稳定的系统脚手架能力迁移到 `/Users/guoc/dev/code_go/src/nai-tizi`。

迁移范围包括：

- 去除 GORM 软删除字段和软删除逻辑。
- 对齐非 `biz_` 基础表模型和基础系统表结构。
- 迁移 API 权限体系，支持菜单权限和接口权限分离。
- 迁移用户/角色双向授权能力。
- 迁移 `pkg` 公共能力分包。
- 集成 MQTT/RabbitMQ 基础能力，默认不启用，业务需要时可直接开启使用。
- 迁移前端系统管理基础页面能力。

不迁移范围：

- lsh-api 的自动门、设备、任务、分享、SN、告警等具体业务功能。
- lsh-api 的业务 MQTT/RabbitMQ 消费逻辑。
- lsh-api dev 环境的真实连接配置值。

## 优先级

### P0 基础结构和表模型

- 删除 `DeletedAt gorm.DeletedAt`。
- 删除 `deleted_at` 查询条件。
- 删除 `Unscoped()` 软删除相关调用。
- 删除软删除插件。
- 新增 `s_api_permission`、`m_role_api_permission`、`m_user_api_permission`。
- 更新 AutoMigrate 基础表列表。

### P1 API 权限体系

- 迁移 API 权限 controller/service/model/request。
- 迁移 API 权限路由。
- 迁移 Casbin 批量策略写入。
- 迁移菜单权限保存时自动同步 API 权限。
- 同步 API 权限初始化 SQL。

### P2 用户/角色授权交互

- 用户管理支持分配角色。
- 角色管理支持分配用户。
- 后端新增角色用户查询、添加、移除接口。
- 前端新增对应弹窗。

### P3 公共能力分包

- 将可复用能力迁移到 `pkg`。
- 内部消息、缓存、任务启动编排放在 `internal`。
- 去掉 `internal/infrastructure` 中不再合理的公共能力堆叠。

### P4 MQTT/RabbitMQ 脚手架能力

- 迁移 MQTT client 基础包。
- 迁移 RabbitMQ manager/producer 基础包。
- 保留消息业务扩展目录。
- 默认配置关闭，业务按需开启。

### P5 前端系统能力

- 迁移 API 权限管理页面。
- 迁移角色/用户授权弹窗。
- 同步 role API、apiPermission API。
- 保持现有后台 UI 风格。

## 执行记录

- 2026-05-17：创建迁移记录，开始执行 P0-P5。
- 2026-05-17：完成后端系统脚手架迁移，删除旧 `internal/infrastructure`，公共能力迁移到 `pkg`，消息扩展保留在 `internal/messaging`。
- 2026-05-17：完成软删除移除，源码、配置、迁移 SQL 中不再使用 `DeletedAt`、`deleted_at`、软删除插件或 `enableSoftDelete`。
- 2026-05-17：完成 API 权限、用户角色双向授权、菜单授权自动同步 API 权限、Casbin 批量策略写入能力迁移。
- 2026-05-17：完成前端 `web-react/src` 按 lsh-api 成熟脚手架基线同步，不再保留老 `nai-tizi` 字段兼容层。
- 2026-05-17：已备份测试库到 `native/backups/scaffold_migration_20260517_130942/`，并直接执行数据库表结构改造，当前 public schema 中 `deleted_at` 字段数量为 0。

## 验证记录

- `native`: `go test ./...` 通过。
- `web-react`: `npm run build` 通过。
- `native`: `rg` 校验 `internal`、`cmd`、`pkg`、`deploy` 中无软删除、旧 `internal/infrastructure`、lsh 自动门业务残留引用。
