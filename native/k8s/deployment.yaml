apiVersion: apps/v1
kind: Deployment
metadata:
  name: quick-admin-api
  labels:
    app: quick-admin-api
spec:
  replicas: 2
  selector:
    matchLabels:
      app: quick-admin-api
  template:
    metadata:
      labels:
        app: quick-admin-api
    spec:
      containers:
      - name: api
        image: ghcr.io/your-org/quick-admin:latest
        ports:
        - containerPort: 8080
          name: http
        env:
        - name: ENV
          value: "production"
        - name: DB_HOST
          valueFrom:
            configMapKeyRef:
              name: quick-admin-config
              key: db_host
        - name: DB_PORT
          valueFrom:
            configMapKeyRef:
              name: quick-admin-config
              key: db_port
        - name: DB_USER
          valueFrom:
            secretKeyRef:
              name: quick-admin-secret
              key: db_user
        - name: DB_PASSWORD
          valueFrom:
            secretKeyRef:
              name: quick-admin-secret
              key: db_password
        - name: DB_NAME
          valueFrom:
            configMapKeyRef:
              name: quick-admin-config
              key: db_name
        - name: REDIS_HOST
          valueFrom:
            configMapKeyRef:
              name: quick-admin-config
              key: redis_host
        - name: REDIS_PORT
          valueFrom:
            configMapKeyRef:
              name: quick-admin-config
              key: redis_port
        - name: JWT_SECRET
          valueFrom:
            secretKeyRef:
              name: quick-admin-secret
              key: jwt_secret
        resources:
          requests:
            memory: "512Mi"
            cpu: "500m"
          limits:
            memory: "1Gi"
            cpu: "1000m"
        livenessProbe:
          httpGet:
            path: /health
            port: 8080
          initialDelaySeconds: 30
          periodSeconds: 10
          timeoutSeconds: 3
          failureThreshold: 3
        readinessProbe:
          httpGet:
            path: /health
            port: 8080
          initialDelaySeconds: 10
          periodSeconds: 5
          timeoutSeconds: 3
          failureThreshold: 3
        volumeMounts:
        - name: logs
          mountPath: /app/logs
      volumes:
      - name: logs
        emptyDir: {}
