apiVersion: v1
kind: ConfigMap
metadata:
  name: quick-admin-config
data:
  db_host: "postgres-service"
  db_port: "5432"
  db_name: "quick_admin"
  redis_host: "redis-service"
  redis_port: "6379"
  app_env: "production"
  log_level: "info"
